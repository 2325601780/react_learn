//项目入口，从这里开始运行

import React from 'react';
import ReactDOM from 'react-dom/client';

import App from './day-02/App';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);

